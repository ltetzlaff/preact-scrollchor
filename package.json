{
  "name": "preact-scrollchor",
  "version": "4.4.0",
  "description": "A Preact component for scroll to #hash links with smooth animations",
  "files": [
    "dist"
  ],
  "repository": "https://github.com/ltetzlaff/preact-scrollchor.git",
  "main": "dist/scrollchor.js",
  "types": "dist/scrollchor.d.ts",
  "scripts": {
    "prepare": "npm run buildOnce",
    "build": "tsc --watch || true",
    "buildOnce": "tsc",
    "lint": "tslint --format stylish --project .",
    "prettier": "prettier --config node_modules/tslint-lt/.prettierrc.yml --write \"{,!node_modules/}**/*.{ts,tsx,css,scss,html,js,jsx,yml,json,md}\"",
    "test": "npm run buildOnce && npm run lint && npm run prettier"
  },
  "keywords": [
    "preact",
    "component",
    "preact-component",
    "scroll",
    "anchor",
    "scrollchor",
    "hash",
    "animate"
  ],
  "peerDependencies": {
    "preact": "^8.4.2"
  },
  "dependencies": {
    "preact": "^8.4.2"
  },
  "devDependencies": {
    "tslint-lt": "^1.8.0",
    "typescript": "^3.3.3333"
  },
  "contributors": [
    "bySabi Files",
    "ltetzlaff"
  ],
  "license": "ISC"
}
